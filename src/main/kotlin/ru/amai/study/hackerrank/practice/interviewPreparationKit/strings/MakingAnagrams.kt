package ru.amai.study.hackerrank.practice.interviewPreparationKit.strings

import java.util.*

fun makeAnagram(a: String, b: String): Int {
        return TODO()
}

fun main() {
    val scan = Scanner(System.`in`)
    val a = scan.nextLine()
    val b = scan.nextLine()

    val res = makeAnagram(a, b)
    println(res)
}
